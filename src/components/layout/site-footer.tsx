export function SiteFooter() {
  return (
    <footer
      style={{
        maxWidth: "1200px",
        margin: "0 auto",
        width: "100%",
        padding: "0 clamp(20px, 5vw, 72px) 32px",
        boxSizing: "border-box",
        fontSize: "13px",
        opacity: 0.6,
      }}
    >
      RallyPoint tournament registration.
    </footer>
  );
}
